name: Build and test
description: Docs build and testing
runs:
  using: composite
  steps:
    - name: Install dependencies and build
      run: yarn && yarn build
      shell: bash
      working-directory: ./www
    - name: Tests
      run: yarn test
      shell: bash
      working-directory: ./www
