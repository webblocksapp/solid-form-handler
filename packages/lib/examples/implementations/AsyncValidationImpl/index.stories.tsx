import type { Meta } from 'storybook-solidjs';
import { AsyncValidationImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: AsyncValidationImpl,
} satisfies Meta<typeof AsyncValidationImpl>;

export default meta;

export const AsyncValidation = {};
