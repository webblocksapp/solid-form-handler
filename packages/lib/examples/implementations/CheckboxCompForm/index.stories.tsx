import type { Meta } from 'storybook-solidjs';
import { CheckboxCompForm } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: CheckboxCompForm,
} satisfies Meta<typeof CheckboxCompForm>;

export default meta;

export const FormWithCheckbox = {};
