import type { Meta } from 'storybook-solidjs';
import { CheckboxImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: CheckboxImpl,
} satisfies Meta<typeof CheckboxImpl>;

export default meta;

export const Checkbox = {};
