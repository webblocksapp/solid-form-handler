import type { Meta } from 'storybook-solidjs';
import { CheckboxesImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: CheckboxesImpl,
} satisfies Meta<typeof CheckboxesImpl>;

export default meta;

export const Checkboxes = {};
