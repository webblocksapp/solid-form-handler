import type { Meta } from 'storybook-solidjs';
import { ComplexFormImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: ComplexFormImpl,
} satisfies Meta<typeof ComplexFormImpl>;

export default meta;

export const ComplexForm = {};
