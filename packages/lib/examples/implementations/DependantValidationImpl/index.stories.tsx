import type { Meta } from 'storybook-solidjs';
import { DependantValidationImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: DependantValidationImpl,
} satisfies Meta<typeof DependantValidationImpl>;

export default meta;

export const DependantValidation = {};
