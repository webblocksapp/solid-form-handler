import type { Meta } from 'storybook-solidjs';
import { FormImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: FormImpl,
} satisfies Meta<typeof FormImpl>;

export default meta;

export const Form = {};
