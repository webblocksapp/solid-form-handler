import type { Meta } from 'storybook-solidjs';
import { MultiSelectImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: MultiSelectImpl,
} satisfies Meta<typeof MultiSelectImpl>;

export default meta;

export const MultiSelect = {};
