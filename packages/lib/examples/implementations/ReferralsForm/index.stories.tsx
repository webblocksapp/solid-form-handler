import type { Meta } from 'storybook-solidjs';
import { ReferralsForm } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: ReferralsForm,
} satisfies Meta<typeof ReferralsForm>;

export default meta;

export const ReferralsFormImpl = {};
