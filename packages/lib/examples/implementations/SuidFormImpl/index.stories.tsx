import type { Meta } from 'storybook-solidjs';
import { SuidFormImpl } from '.';

const meta = {
  title: 'MUI Implementations',
  component: SuidFormImpl,
} satisfies Meta<typeof SuidFormImpl>;

export default meta;

export const SuidForm = {};
