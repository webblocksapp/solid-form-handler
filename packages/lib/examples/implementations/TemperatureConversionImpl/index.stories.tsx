import type { Meta } from 'storybook-solidjs';
import { TemperatureConversionImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: TemperatureConversionImpl,
} satisfies Meta<typeof TemperatureConversionImpl>;

export default meta;

export const TemperatureConversion = {};
