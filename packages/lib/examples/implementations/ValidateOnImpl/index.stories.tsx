import type { Meta } from 'storybook-solidjs';

import { ValidateOnImpl } from '.';

const meta = {
  title: 'BS5 Implementations',
  component: ValidateOnImpl,
} satisfies Meta<typeof ValidateOnImpl>;

export default meta;

export const ValidateOn = {};
